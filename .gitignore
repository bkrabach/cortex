# Build artifacts published through the hub, never committed.
dist/*.apk

# Lane bookkeeping from the overnight build harness.
lane.log

__pycache__/
*.pyc
.venv/

# Package build output (scripts/make-package.sh).
build/
